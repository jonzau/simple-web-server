{
  "name": "simple-web-server",
  "version": "1.0.0",
  "description": "Simple Web Node.js Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http": "0.0.1-security"
  }
}
